## Smovs

Data moving utility for DMS

### Install

```sh
cargo install --git https://github.com/nijo-a/smovs
```

### Usage

Smovs automatically picks up databases from `~/.pgpass` (Should work on all systems)

That's it! Just run `smovs` in your terminal and the TUI should be straight forward
